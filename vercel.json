{
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "framework": "nextjs",
  "buildCommand": "pnpm prisma generate && pnpm run db:migrate:vercel && pnpm next build",
  "installCommand": "pnpm install --frozen-lockfile",
  "env": {
    "PRISMA_GENERATE": "true",
    "NODE_ENV": "production",
    "PRISMA_MIGRATE_ALLOW_RESET": "false"
  },
  "version": 2,
  "regions": ["iad1"],
  "build": {
    "env": {
      "NODE_VERSION": "20.x"
    }
  }
}
